import SectionContainer from "../layout/SectionContainer"

type Props = {}

const Footer = (props: Props) => {
  return (
    <SectionContainer>
        Footer
    </SectionContainer>
  )
}

export default Footer