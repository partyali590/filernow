import SectionContainer from "../layout/SectionContainer"

type Props = {}

const Header = (props: Props) => {
  return (
    <SectionContainer>
        header
    </SectionContainer>
  )
}

export default Header